SET(LIBNAME "asio_helper")
FILE(GLOB SRC_LISTS *.cpp *.h)
ADD_LIBRARY(${LIBNAME} STATIC ${SRC_LISTS})

FIND_PACKAGE(boost REQUIRED asio)
TARGET_LINK_LIBRARIES(${LIBNAME} PRIVATE ${Boost_LIBRARIES})
TARGET_INCLUDE_DIRECTORIES(${LIBNAME} PRIVATE ${Boost_INCLUDE_DIRS})